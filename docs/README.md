# SpecPort GitHub Pages

This directory is a dependency-free static site. GitHub Pages publishes it
from the `main` branch through `.github/workflows/pages.yml`.

The page intentionally mirrors the npm README: it leads with the receiver-first
review-coverage wedge, then explains the contract and agent-skill extensions
without claiming generic code generation or human approval by automation.
